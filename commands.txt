# Commands.txt. What does this mean? 
# In short, collection of commands that are cumbersome to type.

# RUN THESE COMMANDS INSIDE OF CONFIGURATION FOLDER

# This command compiles and 'switches' to home-manager packages and 
# configuration inside the current directory. #{user}@{hostname}
home-manager switch --flake .#neck@nixos

# Rebuilds the entire system configuration. Including boot options,
# system packages, configs, and whatever else. #{hostname}
sudo nixos-rebuild switch --flake .#nixos
